const { resolve } = require("path");

module.exports = {
  config: resolve("dist", "config", "config.js"), // Point to the transpiled config
  "models-path": resolve("dist", "models"), // Transpiled models
  "seeders-path": resolve("dist", "seeders"), // Transpiled seeders
  "migrations-path": resolve("dist", "migrations"), // Transpiled migrations
};
